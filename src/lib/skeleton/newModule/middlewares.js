module.exports = {
  "/" : function(req,res,next){
    // Middlewares are standard Express. The Key is the route on which the middleware will play
    // All children routes will also use the middleware.
  }
}
