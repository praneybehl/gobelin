// Each controller represents an HTTP verb(IE: "get"Controller)

module.exports = {
  "/newModule" : function(req,res){
    // Write your applications here. it works just as you would expect Express to
    // Models are accessible via [models]. (IE: models.Product)
    // Models are unmodified Mongoose Schema, documentation should be ample
    // You can access raw Socket.io via "io" (IE: io.sockets.emit)
    // Have fun!
  }
}
